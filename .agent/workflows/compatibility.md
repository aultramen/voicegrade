---
description: "Run a full tech stack compatibility audit. Scans dependencies, checks versions, and reports conflicts. Read-only — never modifies files without approval."
---

> **This workflow has been merged into `/audit`.**
>
> Run: `/audit compat` for compatibility-only audit, or `/audit` for full health check (security + compatibility).
>
> See: `.agent/workflows/audit.md`
